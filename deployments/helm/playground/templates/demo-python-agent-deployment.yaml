{{- if .Values.demoPythonBot.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "playground.fullname" . }}-demo-python-bot
  labels:
    {{- include "playground.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "playground.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: demo-python-bot
  template:
    metadata:
      labels:
        {{- include "playground.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: demo-python-bot
    spec:
      initContainers:
        - name: wait-control-plane
          image: curlimages/curl:8.12.1
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - >
              until curl -sf "http://{{ include "playground.controlPlane.fullname" . }}:{{ .Values.controlPlane.service.port }}/api/v1/health" >/dev/null;
              do echo "waiting for control plane"; sleep 2; done
      containers:
        - name: demo-python-bot
          image: "{{ .Values.demoPythonBot.image.repository }}:{{ .Values.demoPythonBot.image.tag }}"
          imagePullPolicy: {{ .Values.demoPythonBot.image.pullPolicy }}
          command:
            - sh
            - -c
            - >
              python -m pip install --no-cache-dir --upgrade pip &&
              python -m pip install --no-cache-dir {{ .Values.demoPythonBot.pipPackage | quote }} &&
              python /app/main.py
          ports:
            - name: http
              containerPort: {{ .Values.demoPythonBot.service.port }}
          env:
            - name: PLAYGROUND_URL
              value: "http://{{ include "playground.controlPlane.fullname" . }}:{{ .Values.controlPlane.service.port }}"
            - name: HANZO_NODE_ID
              value: {{ .Values.demoPythonBot.nodeId | quote }}
            - name: PORT
              value: {{ .Values.demoPythonBot.service.port | quote }}
            - name: HANZO_CALLBACK_URL
              value: "http://{{ include "playground.fullname" . }}-demo-python-bot:{{ .Values.demoPythonBot.service.port }}"
            {{- if .Values.apiAuth.enabled }}
            - name: PLAYGROUND_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "playground.apiAuth.secretName" . }}
                  key: {{ .Values.apiAuth.existingSecretKey }}
            {{- end }}
          volumeMounts:
            - name: app
              mountPath: /app
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: app
          configMap:
            name: {{ include "playground.fullname" . }}-demo-python-bot
{{- end }}

