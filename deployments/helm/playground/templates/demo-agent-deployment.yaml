{{- if .Values.demoBot.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "playground.demoBot.fullname" . }}
  labels:
    {{- include "playground.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "playground.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: demo-bot
  template:
    metadata:
      labels:
        {{- include "playground.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: demo-bot
    spec:
      initContainers:
        - name: wait-control-plane
          image: curlimages/curl:8.12.1
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - >
              until curl -sf "http://{{ include "playground.controlPlane.fullname" . }}:{{ .Values.controlPlane.service.port }}/api/v1/health" >/dev/null;
              do echo "waiting for control plane"; sleep 2; done
      containers:
        - name: demo-bot
          image: "{{ .Values.demoBot.image.repository }}:{{ .Values.demoBot.image.tag }}"
          imagePullPolicy: {{ .Values.demoBot.image.pullPolicy }}
          args: ["serve"]
          ports:
            - name: http
              containerPort: {{ .Values.demoBot.service.port }}
          env:
            - name: PLAYGROUND_URL
              value: "http://{{ include "playground.controlPlane.fullname" . }}:{{ .Values.controlPlane.service.port }}"
            - name: HANZO_NODE_ID
              value: {{ .Values.demoBot.nodeId | quote }}
            - name: HANZO_LISTEN_ADDR
              value: ":{{ .Values.demoBot.service.port }}"
            - name: HANZO_PUBLIC_URL
              value: "http://{{ include "playground.demoBot.fullname" . }}:{{ .Values.demoBot.service.port }}"
            {{- if .Values.apiAuth.enabled }}
            - name: PLAYGROUND_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "playground.apiAuth.secretName" . }}
                  key: {{ .Values.apiAuth.existingSecretKey }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 2
            periodSeconds: 5
{{- end }}
