apiVersion: v1
kind: Service
metadata:
  name: playground-demo-bot
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/component: demo-bot
  ports:
    - name: http
      port: 8001
      targetPort: http

