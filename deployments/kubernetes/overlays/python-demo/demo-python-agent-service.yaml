apiVersion: v1
kind: Service
metadata:
  name: playground-demo-python-bot
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/component: demo-python-bot
  ports:
    - name: http
      port: 8001
      targetPort: http

