apiVersion: v1
kind: Service
metadata:
  name: playground-playground
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/component: playground
  ports:
    - name: http
      port: 8080
      targetPort: http
    - name: grpc
      port: 8180
      targetPort: grpc

