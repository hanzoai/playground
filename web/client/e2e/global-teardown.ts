/**
 * Global teardown — runs once after all projects complete.
 * Placeholder for future cleanup (e.g., deleting test accounts, revoking tokens).
 */
async function globalTeardown() {
  // No-op for now. Add cleanup logic here if needed.
}

export default globalTeardown;
