# E2E Test Configuration
# Copy this to .env.e2e and fill in values.
# NEVER commit .env.e2e — it's in .gitignore.

# Target app URL
E2E_BASE_URL=https://app.hanzo.bot

# IAM / hanzo.id credentials
E2E_IAM_SERVER_URL=https://hanzo.id
E2E_IAM_CLIENT_ID=hanzobot-client-id
E2E_IAM_USER_EMAIL=e2e-test@hanzo.bot
E2E_IAM_USER_PASSWORD=<secure-password>
E2E_IAM_ORGANIZATION=hanzo
E2E_IAM_APPLICATION=app-hanzo

# Commerce API (all Hanzo services use IAM auth — JWT works everywhere)
E2E_COMMERCE_API_URL=https://commerce.hanzo.ai
