# {{.ProjectName}}

A Playground agent created with `af init`.

## Quick Start

1. **Install dependencies:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Start the Playground server** (in another terminal):
   ```bash
   af server
   ```

3. **Run the agent:**
   ```bash
   python main.py
   ```

   The agent will auto-discover an available port and register with Playground.

## Test the Agent

### Echo Bot (No AI Required)
```bash
curl -X POST http://localhost:8080/api/v1/execute/{{.NodeID}}.demo_echo \
  -H "Content-Type: application/json" \
  -d '{"input": {"message": "Hello World"}}'
```

### Enable AI Features

1. Uncomment the `ai_config` in `main.py`
2. Set your API key: `export HANZO_API_KEY=your-key`
   - Or use `OPENAI_API_KEY` for direct OpenAI access
   - Routes through api.hanzo.ai by default
3. Uncomment the `analyze_sentiment` bot in `bots.py`
4. Restart the agent

```bash
curl -X POST http://localhost:8080/api/v1/execute/{{.NodeID}}.demo_analyze_sentiment \
  -H "Content-Type: application/json" \
  -d '{"input": {"text": "I love this product!"}}'
```

## Project Structure

- `main.py` - Agent configuration and startup
- `bots.py` - Bot definitions (AI-powered functions)
- `requirements.txt` - Python dependencies
- `.env.example` - Environment variable examples

## Learn More

- [Playground Documentation](https://hanzo.bot/docs)
- [SDK Reference](https://hanzo.bot/docs/sdk)

---

Created by {{.AuthorName}} ({{.AuthorEmail}}) on {{.CreatedAt}}
